#!/usr/bin/env python

from distutils.core import setup

setup(
    name='assignment',
    version='1.0',
    description='Hiring assignment',
    author='',
    author_email='',
    install_requires=[],
    extras_require={
        "dev": [
            "pytest"
        ]
    }
)